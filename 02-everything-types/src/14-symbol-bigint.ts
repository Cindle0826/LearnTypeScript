const oneHundred: bigint = BigInt(10);
const anotherHundred: bigint = 100n;

const firstName: Symbol = Symbol("name");
const lastName: Symbol = Symbol("name");

firstName !== lastName && console.log(123);
